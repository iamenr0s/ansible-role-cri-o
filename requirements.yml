---

# Ansible Requirements for Swap Role
# Install with: ansible-galaxy install -r requirements.yml
# Collections
collections:
  - name: ansible.posix
    version: ">=1.3.0"
  - name: community.general
    version: ">=8.0.0"

# No roles required